const IncomingResolution = ({ navigation, route }: { navigation: any, route: any }) => {
        
        return (
                <>
                </>
        )
}

export default IncomingResolution