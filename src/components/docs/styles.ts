import { StyleSheet } from 'react-native'

import COLORS from '../../assets/colors'

const styleDocument = StyleSheet.create({
        container: {
                padding: 10,
                backgroundColor: 'transparent',
        },
})

export default styleDocument